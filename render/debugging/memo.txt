----------------------------------------------------------------

memoization
- 불필요한 중복 연산을 하지 않기 위해 메모리에 저장하고 가져오는 기법 (dp같은 ㅋㅋ)
- 피보나치 

profiler
- react dev tools설치시 profiler 추가됨
- 막대그래프 영역에 렌더링 횟수와 사유를 알 수 있음
- components에서 컴포넌트별 hooks 등록 확인 가능
- ranked

----------------------------------------------------------------

Word를 React.memo로 감싸서 리렌더링안되게 처리

// false리턴시 항상 리렌더링
// true 리턴시 리렌더링 안함

----------------------------------------------------------------

useMemo
- 연산을 메모아이제이션
- 메모리를 추가적으로 쓸것이다.

----------------------------------------------------------------